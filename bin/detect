#!/usr/bin/env bash
# bin/detect <build-dir>

if [ -n "$METEOR_SUB_DIR"]; then
    BUILD_DIR=$METEOR_SUB_DIR/$1
else
    BUILD_DIR=$1
fi

if [ -d "$BUILD_DIR/.meteor" ]; then
  echo "Meteor app" && exit 0
else
  echo "no" && exit 1
fi
